# LLM provider abstraction — swap these two to change models (config-only).
# Examples: openai/gpt-4o, anthropic/claude-opus-4-8, ollama/llama3.1
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o

# Provider key — set the standard env var for whichever provider you chose.
OPENAI_API_KEY=sk-your-key
# ANTHROPIC_API_KEY=...

# MCP server (FastMCP streamable-http) endpoint
MCP_URL=http://localhost:8001/mcp

# Allowed CORS origins (comma-separated) — the Vite dev server
CORS_ORIGINS=http://localhost:5173

LOG_LEVEL=INFO

# Write one JSONL record per turn to logs/turns.jsonl (query, tool calls + Spotify
# results, final answer, errors). Holds full user messages and model output — set false
# to disable. Also serves as the replay dataset for tests/run_tool_eval.py.
TURN_LOG_ENABLED=true
